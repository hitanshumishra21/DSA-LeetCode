class Solution {
    public int lengthOfLastWord(String s) {
        String newStr = s.trim();
        int length = 0;
        for(int i = newStr.length() - 1;i >=0 && newStr.charAt(i) != ' ' ;i--){
            length++;
        }
        return length;
    }
}
